%h1&= @revision.subject.title
%h2 고치기 

.revision
  - form_for([@revision.forum, @revision.subject, @revision.post, @revision]) do |f| 
    = f.error_messages
    %p
      %textarea{:id => "body", :name => "body", :style => "width: 600px; height:300px;" } 
    %p= f.submit "Create" 
  %br

= link_to 'Back', [@revision.forum, @revision.subject, @revision.post]
